apiVersion: v1
kind: Service
metadata:
  name: crashloop-demo
  namespace: troubleshooting-lab
  labels:
    app: crashloop-demo
spec:
  type: ClusterIP

  selector:
    app: crashloop-demo

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
